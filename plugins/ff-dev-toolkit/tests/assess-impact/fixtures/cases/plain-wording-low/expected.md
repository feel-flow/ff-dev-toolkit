# expected: plain-wording-low — 波及なしの文言修正は LOW

> このファイルはブラインドテストの採点者用。fixture（input.md）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待分類: 文言修正
- 期待影響度: LOW
- 検証する生成器規則: 変更量では判定しない

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **影響度が LOW** であること（MEDIUM 以上と判定したら不合格）。
   波及がない機械的な文言修正のため LOW。
2. 分類が **文言修正** であること。
3. **複数箇所（3 箇所）にまたがることを理由に MEDIUM 以上へ引き上げていない**こと
   （「複数ファイル/複数箇所だから」で影響度を上げたら不合格。影響度は変更量ではなく波及で測る）。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `LOW`
- `波及` または `波及しない` または `波及なし`

## 過剰判定の禁止

- MEDIUM / HIGH と判定したら不合格。この変更は他の箇所へ波及しないため LOW が正。
