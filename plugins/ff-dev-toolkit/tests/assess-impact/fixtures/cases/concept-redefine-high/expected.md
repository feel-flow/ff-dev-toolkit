# expected: concept-redefine-high — 枠組みの再設計は HIGH + 必須手順判定

> このファイルはブラインドテストの採点者用。fixture（input.md）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待分類: 概念再定義
- 期待影響度: HIGH
- 検証する生成器規則: HIGH 判定時の追加アクション

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **影響度が HIGH** であること（LOW/MEDIUM と判定したら不合格）。
   既存の枠組み（認証方式）そのものを変え、後方互換がなく移行を伴うため HIGH。
2. 分類が **概念再定義** であること。
3. HIGH のため、**ADR の要否判定**と**移行計画の要否判定**の両方に言及していること。
4. 必須手順が済むまで**実装に着手しない**旨（着手前ゲート）に触れていること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `HIGH`
- `ADR`
- `移行計画`
- `着手`

## 影響を受ける文書（いずれかへの言及）

- 認証は設計に関わるため `ARCHITECTURE.md` への波及に言及していること
  （`ARCHITECTURE` の語が現れること）。

## 過少判定の禁止

- MEDIUM 以下と判定したら不合格。後方互換のない枠組み変更は HIGH が正。
