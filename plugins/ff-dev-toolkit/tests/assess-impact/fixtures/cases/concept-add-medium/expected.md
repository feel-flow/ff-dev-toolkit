# expected: concept-add-medium — 既存の枠組みを保った要素追加は MEDIUM

> このファイルはブラインドテストの採点者用。fixture（input.md）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待分類: 概念追加
- 期待影響度: MEDIUM
- 検証する生成器規則: MEDIUM/HIGH の境界 = 既存設計の維持可能性

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **影響度が MEDIUM** であること（LOW/HIGH と判定したら不合格）。
   既存の枠組みを保ったまま要素（オプショナルパラメータ）を足すため MEDIUM。
2. 分類が **概念追加** であること。
3. MEDIUM/HIGH の境界＝**既存設計の維持可能性**に基づく判定であること
   （後方互換があり既存設計を維持できる＝HIGH ではない、という筋道が現れること）。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `MEDIUM`
- `概念追加` または `後方互換` または `オプショナル`

## 過剰・過少判定の禁止

- HIGH と判定したら不合格（破壊的変更ではなく後方互換があるため）。
- LOW と判定したら不合格（API 仕様書・コードへ波及するため LOW ではない）。
