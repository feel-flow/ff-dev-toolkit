# expected: ripple-wording-medium — 波及ありの文言修正は MEDIUM

> このファイルはブラインドテストの採点者用。fixture（input.md）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待分類: 文言修正
- 期待影響度: MEDIUM
- 検証する生成器規則: LOW/MEDIUM の境界 = 波及の有無

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **影響度が MEDIUM** であること（LOW と判定したら不合格）。
   文言修正でも、その文字列を CI/コードが解釈している=波及があるため MEDIUM。
2. 分類が **文言修正** であること（見た目の分類は文言修正、影響度は波及ゆえ MEDIUM に上がる、
   という筋道が判定理由に現れること）。
3. 判定理由が **波及の有無**（`gate.sh` が文字列を解釈している事実）に基づくこと。
4. **変更量（1 行・単一ファイル）を根拠に LOW としていない**こと
   （「1 行だから軽微」で LOW にしたら不合格）。
5. 影響を受ける文書・成果物として `gate.sh`（またはリリースゲート/CI）への波及に言及すること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `MEDIUM`
- `波及`
- `gate.sh` または `CI` または `ゲート`

## 過剰判定の禁止

- HIGH と判定したら不合格（既存の枠組み＝ゲート判定の仕組み自体は再設計不要。
  文字列突き合わせ対象を1箇所直せば維持できるため HIGH ではない）。
