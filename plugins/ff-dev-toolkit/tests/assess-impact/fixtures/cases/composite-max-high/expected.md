# expected: composite-max-high — 複合変更は最大影響度を集約する

> このファイルはブラインドテストの採点者用。fixture（input.md）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待分類: 概念再定義
- 期待影響度: HIGH
- 検証する生成器規則: 複数の変更単位が混在する場合

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **全体の影響度が HIGH** であること（LOW/MEDIUM と判定したら不合格）。
   低影響の変更（typo・ログ文言）に紛れた高影響変更（DB スキーマ+移行）を
   見落とさず、最も高い影響度を全体として採ること。
2. 変更単位ごとに分類・影響度を評価し、**最大を全体として採る**筋道が現れること。
3. DB スキーマ変更が **概念再定義 / HIGH**（既存の枠組みを変える・移行を伴い
   ロールバック困難）として評価されること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `HIGH`
- `最も高い` または `最大` または `集約`

## HIGH 判定時の必須手順（いずれかへの言及が必要）

- HIGH のため、ADR の要否判定・移行計画の要否判定に言及していること
  （`ADR` または `移行計画` の語が現れること）。
- 実装着手前に必須手順を済ませる旨（着手前ゲート）に触れていること
  （`着手` の語が現れること。厳密一致は不要だが着手前提が読み取れること）。

## 過検出の禁止

- typo（変更単位1）やログ文言（変更単位2）を単独で MEDIUM 以上に引き上げないこと
  （これらは波及がないため個別には LOW。全体が HIGH なのは変更単位3 の集約による）。
