#!/usr/bin/env bash
# 鮮度由来の赤を出す疑似 suite。行頭の `✗ 鮮度:` マーカーだけを出し、復旧手段は**書かない**。
# 本番の tests/lib/stale-base.sh は復旧手段も出すが、fixture がそれを真似ると、ランナーの
# サマリー行を測るアサーションが fixture 自身の出力に一致して変異を吸収する（実測: 案内の
# 配線を外す変異が SURVIVED した）。fixture は「ガードが無ければ確実に壊れる」内容にする。
# 赤のまま終わるのが正しい — 古い base で判定を諦めて skip へ倒すのは fail-open。
echo "✗ 鮮度: base が先行しているため疑似判定が成立しません（変更起因の赤ではありません）" >&2
exit 1
