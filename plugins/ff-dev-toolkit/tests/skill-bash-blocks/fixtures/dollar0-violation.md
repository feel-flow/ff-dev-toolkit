# fixture: 裸のドル記号 0 検出（violation）

散文の $0 も置換で壊れるので検出する

```bash
line = $0
echo "$0"
# コメント行の $0 も検出する
```

```text
出力例の $0 も検出する（フェンス種別・タグ非依存）
```

インライン code の `$0` も検出する

v=${0} の braced 形も検出する（置換は未実測のため fail-closed）
