# DECISIONS（ADR 見出しが 1 つも無い fixture）

決定ログ表はあるが `## ADR-N:` 見出しを一切書いていない状態。見出し側の抽出が空になり、
表側だけが残っていても fail-closed（rc=2）で落ちることを固定する（`no-log-section` は
決定ログ節が丸ごと無いことで表側の抽出が空になるケースを固定しており、本 fixture はその
逆方向 = 見出し側の抽出が空になるケースを固定する）。

## 決定ログ

| ADR番号 | タイトル     |
| ------- | ------------ |
| ADR-001 | 最初の決定   |
| ADR-002 | 二つ目の決定 |
