#!/usr/bin/env bash
last="${*: -1}"
if [[ "${FAKE_FOOTER_AWK_FAIL:-0}" == 1 && "$last" == */.ff-changelog-footer-input.* ]]; then
  echo "simulated awk read failure" >&2
  exit 9
fi
exec "${FAKE_FOOTER_REAL_AWK:-/usr/bin/awk}" "$@"
