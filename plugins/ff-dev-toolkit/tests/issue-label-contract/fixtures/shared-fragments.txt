;; verify-then-skip ラベル契約のうち、**散文側**の正本テキスト（Issue #292）。
;; bash の実行部分は label-block.txt が連続した行列として持つ（順序・隣接まで固定）。
;; ここには、順序を固定する意味がない独立した文・表だけを置く。
;;
;; 書式:
;;   `;;` 始まりの行と空行 … 注釈（検査対象外）
;;   `~ ` 始まりの行        … 続きの文字列を**部分一致**で探す（長い行の一部を共有する場合）
;;   それ以外の行           … **行全体の完全一致**で探す
;;
;; 既定を行全体一致にしているのは、部分一致だと片側の行末に文を継ぎ足すだけの
;; ドリフトが素通りするため。`~ ` は「両ファイルで文の長さが意図的に違う」箇所に限る。

;; --- verify-then-skip を選ぶ理由（両ファイルで前半の主語が違うため部分一致） ---
~ **存在しないラベル名を渡すと `gh issue create` 自体が失敗する**ため、ラベル名を直書きで固定することもできない（本スキルは複数プロジェクトで動く）。そこで **実在確認 → 存在するものだけ付与 → 省略したものは報告**（verify-then-skip）とする。

;; --- 省略の報告（片方は「手順 7 の完了報告」と具体名を持つため部分一致） ---
~ ラベルが付かなかったこと自体は起票の失敗ではないが、黙って落とすと「付いているはず」という誤解が残る。
理由は `label_lookup_failed` で書き分ける。**「不在」と「照会失敗」を混同しない**:
| `label_lookup_failed` | 報告する理由 |
| `0` | 対象リポジトリに存在しないため省略した |
| `1` | **ラベル一覧を信用できなかった**ため、実在を確認できず省略した（取得失敗・空・取得上限到達のいずれか） |
照会が失敗しただけなのに「存在しない」と断定すると、運用者が実在するラベルを再作成して重複ラベルが生える。なおラベルが 1 つも無いリポジトリも `1`（確認できなかった）に倒れる — 「本当に 0 件」と「一覧が取れていない」を出力から区別できないため、重複ラベルを生やさない側へ倒すのが既定。

;; --- priority の判定基準（表は行全体一致） ---
#### priority の判定基準
ラベル名はプロジェクト依存だが、判定の軸は共通にする。
| 段階 | 基準 |
| critical | 本番で実害が進行中（障害・データ破損・コンプライアンス違反が現在形） |
| high | 利用者に見える不具合、または開発全体をブロックする（品質ゲートが恒常的に赤、標準手順が実行不能） |
| medium | 通常対応。実バグ・整合性リスクだが実害は顕在化していない |
| low | リファクタ・保留可・実需要待ち |
;; out-of-scope-issue は同じ行に「レビュー Suggestion 由来の…」を続けるので部分一致
~ 迷ったら medium。

;; --- bash 3.2 の空配列展開と gh issue create への受け渡し ---
;; 前半の導入文が両ファイルで異なるため、警告本体だけを部分一致で共有する
~ **macOS 標準の bash 3.2 では、`set -u` 下で空配列を `"${label_args[@]}"` と展開すると `unbound variable` で落ちる**。空配列になるのは「ラベル一覧を取得できなかった」「実在するラベルが 1 つも無かった」という**続行したい fail-soft の経路**なので、素直に書くと復旧側だけが落ちる。
  ${label_args[@]+"${label_args[@]}"} \

;; --- 意図的複製であることの相互参照（片方だけ直させないための導線） ---
~ tests/issue-label-contract/verify.sh
~ **fixture に載せた契約行を片方だけ書き換える／削除すると red になる**（fixture 未収載の複製文までは検出しない）ので、変更は必ず両方へ入れること。

;; --- 起票ブロックの状態出力（Issue #297: 報告はこの出力を写す。両ファイルで同一行） ---
;; ラベルは 1 行 1 件（APPLIED_LABEL= / SKIPPED_LABEL=）。ラベル名は空白を含みうるため、
;; 1 行に並べる集約形式（APPLIED=...）へ戻すと境界を復元できなくなる。
printf 'ISSUE_URL=%s\n' "$issue_url"
printf 'LABEL_LOOKUP_FAILED=%s\n' "$label_lookup_failed"
  printf 'APPLIED_LABEL=%s\n' "$lbl"
  printf 'SKIPPED_LABEL=%s\n' "$lbl"
