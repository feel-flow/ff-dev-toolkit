# expected: na-clean-pass — 兆候なし未作成は N/A で分母から除外し達成

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 達成
- 検証する生成器規則: N/A と判定した条件付き文書（DOMAIN・PATTERNS・TESTING・DEPLOYMENT）はスコアの分母に入れない

## 入力の要旨

- 必須3文書（MASTER / PROJECT / ARCHITECTURE）のみが存在し、必須セクションを満たす。
- 条件付き4文書（DOMAIN / PATTERNS / TESTING / DEPLOYMENT）は未作成。
- `project-context.md` により、テストコード・CI 設定・本番環境・散在するビジネスルール等の**必要性の兆候はいずれも無い**ことが固定されている。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. 条件付き4文書をすべて **➖ N/A**（未達ではない）として扱うこと。
   兆候がないため未作成でも減点しない（❌ にしたら不合格）。
2. **N/A の文書をスコアの分母に入れていない**こと（全体スコア＝充足÷判定対象、N/A 除外）。
3. **総合判定が「達成」**であること（❌ が1つも無いため。「未達」としたら不合格）。
4. 必須3文書を存在・充足として認識していること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `N/A` または `➖`
- `達成`
- `分母` または `除外` または `スコア`

## 過剰判定の禁止

- 条件付き文書の未作成を ❌（不足）にしたら不合格（兆候がないため N/A が正）。
- 「7文書すべての整備」を根拠に減点したら不合格（兆候なし未作成は N/A で減点しない）。
