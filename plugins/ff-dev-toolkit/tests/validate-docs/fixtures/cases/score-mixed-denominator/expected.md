# expected: score-mixed-denominator — ❌ と N/A が混在しても N/A は分母から除外する

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 未達
- 検証する生成器規則: **全体スコア** = 充足項目数 ÷ 判定対象項目数

## 入力の要旨

- 必須3文書（MASTER / PROJECT / ARCHITECTURE）は存在する。
- ARCHITECTURE.md の「技術スタック」は存在するが、各技術の**バージョン明記が不足**している。
- `project-context.md` により、条件付き4文書（DOMAIN / PATTERNS / TESTING / DEPLOYMENT）は未作成かつ必要性の兆候もないことが固定されているため N/A。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. ARCHITECTURE.md の技術スタックを **❌（バージョン明記不足）** として指摘すること。
2. 条件付き4文書を **➖ N/A** として扱い、スコアの分母から除外すること。
3. スコア算出で N/A を分母に含めず、判定対象は必須3文書と存在する文書・必須セクション側だけで数える方針を明示すること。
   具体的な分数は実装や出力粒度に依存するため固定しないが、`N/A を除外` と `充足項目数 ÷ 判定対象項目数` の考え方が読み取れること。
4. **総合判定が「未達」**であること（❌ が1つでも残るため。スコアが高くても「達成」にしない）。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `技術スタック`
- `バージョン`
- `N/A` または `➖`
- `分母` または `除外` または `スコア`
- `未達`

## 過剰判定の禁止

- DOMAIN / PATTERNS / TESTING / DEPLOYMENT を兆候ありとして ❌ にしたら不合格（この入力には必要性の兆候を置いていない）。
- スコアが参考値であることを無視し、❌ が残っているのに「達成」としたら不合格。
