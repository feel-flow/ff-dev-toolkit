# expected: synonym-heading-evidence — 同義見出しを内容で充足判定し根拠を出力

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 達成
- 検証する生成器規則: 見出しの文字列一致ではなく、内容の責務で判定する

## 入力の要旨

- フォルダ番号が揺れている（PROJECT.md は `01-business/` 配下）。→ 許容される。
- PROJECT.md の必須セクションが**同義見出し**で書かれている:
  対象ユーザー→「ステークホルダー分析」、主要機能→「要件定義 > 機能要件」、
  非機能要件→「要件定義 > 品質要件」、スコープ外→「スコープ定義」。
- MASTER.md も文書索引→「関連ドキュメント」、重要な制約→「前提」+「コード生成ルール」で表現。
- `project-context.md` により、条件付き4文書は未作成かつ兆候なしであることが固定されている（N/A）。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. フォルダ番号の揺れ（01-context vs 01-business）を許容し、PROJECT.md を検出すること
   （見つからない扱いにしたら不合格）。
2. 同義見出しを**内容の責務で充足判定**すること（見出し名が違うだけで ❌ にしたら不合格）。
3. 充足と判定した項目について、**対応見出しと内容の要旨（根拠）を出力に添える**こと
   （例:「対象ユーザー ← ステークホルダー分析（利用者/運用者）」）。根拠なしで ✅ だけ並べたら不合格。
4. **総合判定が「達成」**であること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `ステークホルダー` または `要件定義` または `スコープ定義` または `関連ドキュメント`
- `達成`

## 過剰判定の禁止

- 「対象ユーザー」「主要機能」「非機能要件」「スコープ外」という**見出し文字列そのもの**が
  無いことを理由に ❌ にしたら不合格（内容で判定するのが正）。
