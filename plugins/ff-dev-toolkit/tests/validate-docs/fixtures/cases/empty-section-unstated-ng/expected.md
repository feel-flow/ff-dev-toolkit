# expected: empty-section-unstated-ng — 状態明記のない空セクションは ❌

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 未達
- 検証する生成器規則: 明記がなければ ❌（空セクション）として指摘する

## 入力の要旨

- 必須3文書（MASTER / PROJECT / ARCHITECTURE）は存在する。
- MASTER.md の必須セクション「重要な制約」は**見出しのみで実質空**、かつ
  「該当なし」「未定」などの状態明記が**ない**。
- `project-context.md` により、条件付き4文書は未作成かつ必要性の兆候もないことが固定されている（N/A）。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. MASTER.md「重要な制約」を **❌（空セクション）**として指摘すること。
   状態明記がない実質空セクションは ❌（✅ にしたら不合格）。
2. **総合判定が「未達」**であること（❌ が残るため。「達成」としたら不合格）。
3. 空セクションの解消（記入するか「該当なし」等の状態明記）を推奨アクションに含めること。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `重要な制約`
- `空` または `❌` または `状態` または `明記`
- `未達`

## 過剰判定の禁止

- 「重要な制約」に状態明記があると誤認して ✅ にしたら不合格（明記は存在しない）。
