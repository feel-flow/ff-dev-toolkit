# expected: domain-signs-not-na — 兆候ある未作成文書は N/A ではなく ❌

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 未達
- 検証する生成器規則: DOMAIN 未作成なのに、ビジネスルール・エンティティ定義が他の文書やコードコメントに散在している

## 入力の要旨

- 必須3文書（MASTER / PROJECT / ARCHITECTURE）は存在し、必須セクションを満たす。
- DOMAIN.md は**未作成**。ただし ARCHITECTURE.md と `project-context.md` に状態遷移・差し戻し可否などの
  ビジネスルールが散在している（＝DOMAIN の必要性の兆候あり）。
- `project-context.md` により、PATTERNS / TESTING / DEPLOYMENT は未作成かつ必要性の兆候もないことが固定されている。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. **DOMAIN.md を ➖ N/A としない**こと。兆候（ビジネスルールの散在）があるため
   **❌（不足）**として報告し、作成を求めていること。
2. 兆候のない PATTERNS / TESTING / DEPLOYMENT は **➖ N/A**（未達ではない）として扱い、
   スコアの分母から除外していること。
3. **総合判定が「未達」**であること（❌ が1つでも残るため。「達成」としたら不合格）。
4. 必須3文書は存在・充足として認識していること（3文書を未達扱いにしたら不合格）。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `DOMAIN`
- `❌` または `不足` または `作成`
- `未達`

## 過剰判定の禁止

- DEPLOYMENT / PATTERNS / TESTING を「兆候あり」と誤検出して ❌ にしたら不合格
  （これらの兆候＝CI 設定・コーディング規約の複数言及・テストコードは入力に存在しない）。
