# expected: empty-section-stated-ok — 状態明記のある空セクションは ✅

> このファイルはブラインドテストの採点者用。fixture（docs/ セット）を処理する実行エージェントには渡さないこと。

<!-- 機械照合フィールド（verify.sh が解析する。`- ラベル: 値` 形式・半角コロン+スペース区切り） -->
- 期待総合判定: 達成
- 検証する生成器規則: 「該当なし」「未定」などの状態が明記されていれば ✅

## 入力の要旨

- 必須3文書（MASTER / PROJECT / ARCHITECTURE）+ DOMAIN.md が存在し、必須セクションを満たす。
- DOMAIN.md の「状態遷移」セクションは実質空だが、**「該当なし（…状態を持たない…）」と状態が明記**されている。
- `project-context.md` により、PATTERNS / TESTING / DEPLOYMENT は未作成かつ必要性の兆候もないことが固定されている（N/A）。

## 判定規則

- 不合格項目に1件でも該当したら総合 FAIL。
- 各項目は本ファイルの基準のみで判定し、採点者の裁量で基準を追加しない。

## 合格条件（必須）

1. DOMAIN.md「状態遷移」を **✅（状態明記あり）**として扱うこと。
   空セクションでも「該当なし」等の状態が明記されていれば ✅（❌ にしたら不合格）。
2. 必須3文書 + DOMAIN が存在・充足として認識されていること。
3. 兆候のない PATTERNS / TESTING / DEPLOYMENT を **➖ N/A** として扱うこと。
4. **総合判定が「達成」**であること（❌ が1つも無いため。「未達」としたら不合格）。

## 期待キーワード（実行結果に含まれるべき語のいずれか）

- `状態遷移`
- `該当なし` または `状態明記` または `✅`
- `達成`

## 過剰判定の禁止

- 「状態遷移」を空セクション扱いで ❌ にしたら不合格（状態明記があるため ✅ が正）。
