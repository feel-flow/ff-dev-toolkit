> このファイルは spec-driven スキルの動作検証用 fixture。架空プロジェクト「在庫アラート機能」の
> 機能要求メモという設定。同ディレクトリの `sample-docs/` が対象プロジェクトの既存仕様文書
>（PROJECT.md / DOMAIN.md のみ存在する最小構成）を模している。
> 使い方: 「fixtures/sample-feature-request.md を spec-driven で進めて。対象プロジェクトの仕様文書は
> fixtures/sample-docs/ にある。G0〜G2 まで進めて」と依頼し、バッチ実行（対話補完なし）で以下を確認する:
>
> 1. ゲート進行表 `spec-driven-gates-issue-123-{初回実行日YYYYMMDD}.md` が作られる
>    （Issue 番号があるため slug は issue-123。タスク名からの導出をしない）
> 2. 進行表の必須構成（タスクサマリー〔メタ情報欄付き〕/ ゲート進行表〔状態・チェック結果・証拠・日時〕/
>    受け入れ基準と検証結果 / 未解決事項 SD-n〔本文出現順・双方向参照〕）が揃う
> 3. G0: 要求の曖昧点（「見やすく」「必要な人に」「すぐ」）が**実装案でなく質問（SD-n）**になり、
>    回答なしで安全に進められる範囲にスコープが絞られる。受け入れ基準 3〜5 個が観測可能な形で書かれる
> 4. G1: 変更の書き先が判断マトリクスで特定され（閾値ルール → DOMAIN.md、通知機能 → PROJECT.md 等）、
>    **コードより先に仕様文書の更新案**が示され、G1 の状態が「提案通過」と記録される（依頼範囲が G2 まで
>    のため適用はしない）。更新案には frontmatter の更新（version・updated・changeImpact〔小文字〕）が
>    含まれる。影響度が評価される（既存ルール変更を含むため MEDIUM 以上の判定と関連文書の確認リストが
>    期待される）
> 5. G2: 実装が検証可能な単位に分解され、受け入れ基準との対応表がある。触らない範囲が明示される
> 6. sample-docs にない事実（既存の通知基盤の仕様・ユーザー数・在庫データのスキーマ詳細）が捏造されない
> 7. G3〜G4 は「未着手」として進行表に残る（依頼範囲は G2 まで。勝手に実装まで進めない）
>
> メモの日付は過去日で固定してあり、実行日より未来になることはない。

# 機能要求メモ: 在庫アラート機能（Issue #123）

- 起票日: 2026-06-28
- 起票者: 営業部門マネージャー
- 対象プロジェクト: 社内在庫管理システム（仕様文書は docs/ = このfixtureでは sample-docs/）

## 要求（起票文そのまま）

在庫が少なくなったら、必要な人にすぐ分かるようにしてほしい。今は月次の棚卸しで気づくので、
売れ筋商品の欠品が起きてから発注することになっている。画面でも見やすくしてほしい。

## 補足（口頭で聞けたこと）

- 「少なくなったら」の基準は商品によって違うらしい（回転率が違うため）。具体的な数字は決まっていない
- 「必要な人」が誰か（発注担当だけか、営業も含むか）は起票者も即答できなかった
- 「すぐ」がリアルタイムか日次かは未確認
- 既存の DOMAIN.md には在庫数の定義と発注ルールの一部が書かれている
