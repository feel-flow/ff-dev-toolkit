> このファイルは sample-harness-design.md の**検証用期待値**。fixture テストの検証者（採点者）だけが読む。
> **レビューを実行するエージェントには渡さないこと**（渡すとブラインドテストにならない）。

# 期待値: sample-harness-design.md のレビュー結果

## 合格条件

1. レポートが `harness-review-juchu-oto-kiban-{実行日YYYYMMDD}.md` になる
   （システム名「受注応答基盤」〔じゅちゅうおうとうきばん〕の読みのヘボン式導出。長音省略（おうとう→oto）の検証を含む）
2. 5セクション構成（サマリー〔メタ情報欄付き〕/ 指摘一覧〔ID・該当箇所・根拠・影響・推奨対応〕/
   観点別チェック表〔OK/NG/情報不足/対象外。NG は指摘 ID・情報不足は HR-n を参照〕/ 良い点 /
   確認事項 HR-n〔出現順・誰に確認するか〕）が揃い、バッチ実行時は番号なし付録節「次のアクション」が
   末尾にある（5セクションに数えない・文書調）
3. 仕込んだ Critical 級欠陥 3 系統が Critical として検出される:
   (a) 停止条件・停止権限の不在（24時間自律稼働） (b) 無限リトライ（成功するまで再試行）
   (c) 権限過剰（基幹システムの管理者アカウント + フェーズ2の承認なし自動送信。1指摘への統合・分割は不問）
4. 仕込んだ Warning 級欠陥の過半が検出される: 役割・書き込みスコープ未定義の5体構成 /
   LLM API の直接呼び出し散在 / 自己申告完了 / 全履歴・全顧客DBのコンテキスト渡し / 校正ループ上限なし
5. 良い設計 3 点が指摘されず「良い点」に記録される: やらないことの明記（与信・値引き判断）/
   フェーズ1 の人間承認付き段階導入 / クレーム・法務メールの人間直送分岐
   （フェーズ2の承認廃止は「段階導入の一部」として免罪されないこと）
6. 「別途検討」と明記されたログ・監視、および記載のないテスト・回帰の戦略が「未実装」と断定されず、
   情報不足（HR-n）として本番化前の確認事項になる
7. 設計書にない実装詳細・運用実態が捏造されない。指摘には根拠（観点+アンチパターン名）が付く
